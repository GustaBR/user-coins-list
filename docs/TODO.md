# TO-DO List
**High Priority:**
- Looking into adding a cleaner way of accessing the admin area to the UI.

**Other tasks:**
- Add newest completions to Database (preferably close to MVP launch).
- Looking into adding a dark theme (using CSS variables).
- Adding error handling and status codes to the API.
- Looking into adding a simple about page.