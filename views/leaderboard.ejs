<!DOCTYPE html>
<html lang="en">

    <%- include("./partials/head.ejs") %>

<body>

    <%- include("./partials/nav.ejs") %>

    <main>
        <% players.forEach((player) => { %>
            <div class="player-card">
                <a href="/players/<%= player.name %>"><p><%= player.name %></p></a>
            </div>
        <% }); %>
    </main>

</body>
</html>